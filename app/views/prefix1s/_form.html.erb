<%= simple_form_for @prefix1, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend>คำนำหน้า 1</legend>

    <%= f.input :prefix1 %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', prefix1s_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
